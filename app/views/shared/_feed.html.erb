<% if @feed_items.any? %> <!-- if there are any feeds items in the db for the user do the following -->
	<ol class = "microposts">
		<%= render partial: 'shared/feed_item', collection: @feed_items %>
	</ol>
	<%= will_paginate @feed_items %>
<% end %>